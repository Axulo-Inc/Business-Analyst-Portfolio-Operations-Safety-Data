name: Portfolio Update Check

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Display Portfolio Information
      run: |
        echo "🎯 Portfolio Status Check"
        echo "========================"
        echo ""
        echo "📊 Repository: ${{ github.repository }}"
        echo "🌿 Branch: ${{ github.ref_name }}"
        echo "📅 Last updated: $(date)"
        echo ""
        echo "📁 Case Studies Found:"
        count=0
        for dir in case-studies/*/; do
          if [ -d "$dir" ]; then
            dir_name=$(basename "$dir")
            echo "  ✓ $dir_name"
            ((count++))
          fi
        done
        echo ""
        echo "📈 Total Case Studies: $count"
        echo "✅ Portfolio structure is valid"
        echo ""
        # Check for README files
        echo "📄 README Files:"
        find . -name "README.md" -type f | head -5 | while read file; do
          echo "  - $file"
        done
