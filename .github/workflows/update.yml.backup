name: Portfolio Update Check

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Portfolio Status Check
      run: |
        echo "🎯 Portfolio Status Check"
        echo "========================"
        echo ""
        echo "📊 Repository: ${{ github.repository }}"
        echo "🌿 Branch: ${{ github.ref_name }}"
        echo "📅 Last updated: $(date)"
        echo ""
        echo "📁 Case Studies Found:"
        ls -d case-studies/*/ 2>/dev/null | while read dir; do
          dir_name=$(basename "$dir")
          echo "  ✓ $dir_name"
        done
        echo ""
        echo "📄 README Files:"
        find . -name "README.md" -type f | head -3
        echo ""
        echo "✅ Portfolio structure is valid"
